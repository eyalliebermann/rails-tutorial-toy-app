<%= form_with(model: microblog, local: true) do |form| %>
  <% if microblog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microblog.errors.count, "error") %> prohibited this microblog from being saved:</h2>

      <ul>
      <% microblog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :microblog_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :microblog_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
